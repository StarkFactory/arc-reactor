# v5.8.2

Release date: 2026-03-16

## Highlights

- **CLAUDE.md accuracy fixes**:
  - Corrected 7 documentation-code mismatches found during microscopic audit (#351):
    - `max-tools-per-request`: 20 to 30 (matches `application.yml`).
    - `llm.temperature`: 0.3 to 0.1 (matches `application.yml`).
    - `guard.rate-limit-per-minute`: 10 to 20 (matches `application.yml`).
    - `guard.rate-limit-per-hour`: 100 to 200 (matches `application.yml`).
    - Removed phantom `rag.grading.enabled` entry (does not exist in code).
    - Fixed `user-memory.inject-into-prompt` to `memory.user.inject-into-prompt`.
    - Updated `SpringAiAgentExecutor` line count from ~1,060 to ~555.

## Compatibility and Migration

- Documentation-only changes. No code, API, or configuration changes from v5.8.1.

## Operational Notes

- Version bumped to `5.8.2`.
- Tag `v5.8.2` triggers GitHub Release and Docker publication workflows.
