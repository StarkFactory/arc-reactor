# v3.8.2 Release Notes

Date: 2026-02-17

## Highlights

- Completed documentation audit and consistency pass across EN/KO docs.
- Added package-style README indexes for each docs category.
- Added missing EN Slack operations docs:
  - hardening checklist
  - ops runbook
- Modernized root `README.md` for current multi-module runtime layout.

## Operational Impact

- Faster onboarding path: docs now expose category entrypoints first.
- Root README examples now align with current module boundaries (`:arc-app` runtime).
- MCP onboarding guidance now follows REST registration flow.

## Upgrade Notes

- Start from docs package index: `docs/en/README.md`.
- For runtime startup, use `./gradlew :arc-app:bootRun`.
- For MCP server registration, use `/api/mcp/servers` admin API.
