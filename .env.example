# ===========================================
# Arc Reactor - Environment Variables
# ===========================================
# 이 파일을 .env로 복사하여 사용하세요:
#   cp .env.example .env
#
# .env 파일은 .gitignore에 포함되어 있어 커밋되지 않습니다.

# ----- LLM Provider (택 1) -----
# Gemini (기본값)
GEMINI_API_KEY=your-gemini-api-key

# OpenAI (build.gradle.kts에서 compileOnly → implementation 변경 필요)
# OPENAI_API_KEY=your-openai-api-key

# Anthropic (build.gradle.kts에서 compileOnly → implementation 변경 필요)
# ANTHROPIC_API_KEY=your-anthropic-api-key

# ----- PostgreSQL (선택, docker-compose 사용 시) -----
# Memory를 InMemory 대신 PostgreSQL에 영구 저장하려면 활성화
# DB_HOST=localhost
# DB_PORT=5432
# DB_NAME=arcreactor
# DB_USER=arc
# DB_PASSWORD=arc

# ----- Server (선택) -----
# SERVER_PORT=8080
