apiVersion: v1
kind: Service
metadata:
  name: {{ include "arc-reactor.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "arc-reactor.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "arc-reactor.selectorLabels" . | nindent 4 }}
